
  
// // Handle incoming events from the client
// const handleTyping = ({ roomId }, socket) => {
//     let skt = socket.broadcast;
//     skt = roomId ? skt.to(roomId) : skt;
//     skt.emit("typing-from-server");
//   };
  
//   // Handle incoming events from the client
//   const handleTypingStopped = ({ roomId }, socket) => {
//     let skt = socket.broadcast;
//     skt = roomId ? skt.to(roomId) : skt;
//     skt.emit("typing-stopped-from-server");
//   };
  
//   module.exports = { handleTyping, handleTypingStopped };
  
  